Show the current issues status of this repo.

Run exactly:

    python3 scripts/todo_status.py --full

Paste the ENTIRE output verbatim inside one ``` block — do not summarize,
trim, reorder or reformat it; every line exactly as printed. This is NOT an
analysis, it is a listing: do not open individual issue files unless the user
asks about one afterwards. If the script prints nothing, this repo has no
`docs/issues/` layout — say so and stop.

After the block, end with one short question: "Which one do you want to work
on?"
